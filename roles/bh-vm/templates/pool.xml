<pool type='logical'>
  <name>{{ pool.name }}</name>
{% for pv in pool.pvs %}
  <source>
    <device path='{{ pv }}'/>
    <name>{{ pool.vg }}</name>
    <format type='lvm2'/>
  </source>
{% endfor %}
</pool>
