# Generated by ansible.

auto {{ bridge.name }}
iface {{ bridge.name }} inet static
  address {{ bridge.ip | ipaddr('address') }}
  netmask {{ bridge.ip | ipaddr('netmask') }}
  {{ ('gateway ' + bridge.gw) if bridge.gw is defined else '' }}
  {{ ('bridge_ports ' + bridge.bridge_port) if bridge.bridge_port is defined else '' }}
  {{ ('dns-nameservers ' + bridge.ns) if bridge.ns is defined else '' }}
